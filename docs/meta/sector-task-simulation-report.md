# Sector Task Simulation Report — Dragons Alliance IDE

Skeleton created 2026-07-07 (pre-build). Each sector gets 5 simulated operator tasks, executed
against the BUILT bundle over CDP after the construction wave; results are recorded per task as
`task · expected · actual · button used · PASS/FAIL · fix applied`. No task is marked PASS
without a real observed effect.

## Status: PENDING EXECUTION (will be filled by the verification harness after the build wave)

### 1. Terminal — planned sims
1. start/verify worker (+ Worker ▸ zsh → pane appears)
2. run health (Tools ▸ Run Health Check → honest toast)
3. check logs (Rooflow ▸ Open Logs → Audit)
4. detect idle/running (worker status pills reflect real state)
5. recap next command (idle-recap overlay content real)

### 2. Agents
1. inspect active agents (roster + health badges)
2. launch agent (Launch → real terminal) or disabled reason
3. broadcast (confirm dialog → sent count) or disabled reason
4. mission control (transcript streams)
5. transcripts/logs (Audit entries exist)

### 3. Code
1. open file (tree → Monaco)
2. search code (palette file index)
3. build/typecheck (action bar arms real terminal in repo)
4. explain file (Ask agent → claude armed with file prompt)
5. patch suggestion (agent flow; no auto-modify)

### 4. Neuromap
1. open graph (nodes render)
2. search node (search box filters)
3. inspect node (inspector shows real frontmatter/backlinks)
4. filter projects (layer segs)
5. explain relations (Diag counts + edges real)

### 5. Drive
1. open folder tab (honest gate if signed out)
2. search file (Drive search or honest gate)
3. open Obsidian note (vault open real)
4. sync/index (Vault sync real git)
5. missing docs surfaced honestly (no fake listing)

### 6. Metrics
1. show top 5 metrics (Dragon Metrics primary row — real-computed)
2. secondary metrics (probe-derived or "needs instrumentation")
3. detect anomaly (error/attention counts real)
4. reason/understanding (explanation panel cites its data sources)
5. export report (copy diagnostics to clipboard — real)

### 7. Preview
1. detect browsers (real /Applications scan: expect Chrome, Brave, Firefox, Safari, Neo on this machine)
2. open Chrome (real `open -a`)
3. open Brave/other found (real)
4. preview route (iframe/Neo real)
5. login/session note (honest copy: user logs in manually; no credential access)

### 8. Creative
1. show top tools (image/video slots visible)
2. image workflow (slot → SETUP_REQUIRED → link to API Power Center)
3. video workflow (same honest path)
4. connect API settings (link lands in Settings ▸ API Power Center)
5. open asset library (gallery — honest empty until real assets)

### 9. Guide
1. first-time tour (Start Tour steps through sectors for real)
2. explain Superpowers (section/chat answer)
3. explain Settings/API Power Center (section/chat answer)
4. explain Preview (section/chat answer)
5. troubleshoot buttons (routes to Quick Guide troubleshooting)

Results appended below after execution.
